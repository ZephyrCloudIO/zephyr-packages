{
  "name": "rolldown-mf",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:host": "pnpm --filter rolldown-mf-host run dev",
    "dev:remote": "pnpm --filter rolldown-mf-remote run dev",
    "dev": "npm-run-all -l --parallel dev:remote dev:host"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
