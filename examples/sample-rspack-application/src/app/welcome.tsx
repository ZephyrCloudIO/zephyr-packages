export function Welcome({ title }: { title: string }) {
  return (
    <main>
      <h1>{title}</h1>
      <p>Rspack sample application deployed with Zephyr.</p>
    </main>
  );
}

export default Welcome;
