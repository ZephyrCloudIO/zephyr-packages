# nitro-preset-cloudflare example

Nitro app using `zephyr-nitro-preset` with custom options and `cloudflare_module` base preset.

## Build and verify preset output

```bash
pnpm -C examples/nitro-preset-cloudflare build
cat examples/nitro-preset-cloudflare/.output/.zephyr/cloudflare-build.json
```
