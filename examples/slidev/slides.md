---
theme: default
title: Slidev + Zephyr
---

# Slidev + Zephyr

This example uses `vite-plugin-zephyr`.

---

## Why this example exists

- Slidev runs on top of Vite
- Zephyr integration lives in `vite.config.ts`
- `with-zephyr` can scaffold this config for fresh Slidev apps
